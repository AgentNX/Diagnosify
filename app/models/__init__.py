# This file initializes the models module by importing the SymptomLog class.
# It allows other parts of the application to access the models by importing from the models package.
from .models import SymptomLog